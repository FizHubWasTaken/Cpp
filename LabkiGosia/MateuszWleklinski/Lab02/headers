#include <cmath>
#include <iostream>
#include <vector>

bool isPrime(int num);
void writePrimeNumbers(int range);

void Eratosthenes(int num);

void linear_sieve(int num);
int table_index(int *tab, int range, int value);

double f(double x);
double integral(double a, double b, double dx=0.1);